Fixes ARM64 float precision bug in Terraria's slope collision code. Replaces speed-dependent Math.Abs(Velocity.X) tolerance with a fixed 1-pixel constant in both TileCollision and SlopeCollision. Install on server for NPC/mob fix, on client for player fix.
